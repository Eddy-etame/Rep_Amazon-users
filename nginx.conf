server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Fallback SPA : toute route Angular inconnue renvoie index.html (routage côté client).
  location / {
    try_files $uri $uri/ /index.html;
  }
}
